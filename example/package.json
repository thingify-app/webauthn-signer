{
    "name": "webauthn-signer-example",
    "scripts": {
        "build": "rm -r dist/ && parcel build index.html --public-url ./",
        "serve": "parcel index.html"
    },
    "private": true,
    "devDependencies": {
        "parcel-bundler": "^1.12.4",
        "typescript": "^4.0.5"
    },
    "browserslist": [
        "last 1 Chrome version"
    ],
    "dependencies": {
        "webauthn-signer": "file:../client_crypto/",
        "webauthn-signer-server": "file:../server/"
    }
}
